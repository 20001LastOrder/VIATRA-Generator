plugins {
    // Apply the java-library plugin to add support for Java Library
    id 'viatra-solver.base'
}

project.ext.set('nativeLibsDir', 'lib')

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}